{
  "name": "lock-x",
  "version": "1.1.0",
  "description": "Block distracting sites when Claude Code is idle",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "DEBUG=1 bun run server.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0"
  }
}
